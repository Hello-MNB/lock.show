# SECURITY BOUNDARY MATRIX (W4-5 / T-39 · spec §13.5)

One page: **surfaces × actors** — what each actor can reach, the *enforcing mechanism* (with file:line), and the *test that proves it*. Sourced only from code/migrations/spec; a cell that cannot be proven by an existing check carries **`?`** and a numbered follow-up (bottom). Legend below the table.

| Surface ↓ / Actor → | anon | authed artist (org) | buyer / no-account | confirmer-token | operator | service-role |
|---|---|---|---|---|---|---|
| **Public Passport read** (`GET /api/passport/:id` → `server/index.js:433-465`; page `/p/:id`) | Buyer-safe snapshot of **published** artists only; unpublished → 404 (`server/index.js:444`). Mech: live `published` re-check + snapshot built by `buildSafePayload` — explicit column lists, `artist_approved=true` (`server/index.js:378`), raw `expires_at` stripped → `stale` label (`server/index.js:382-389`). Test: **GR-1/GR-4**; 404-on-unpublished **? → F1** | Same payload as anon — route takes no auth; owner's private fields are NOT in the snapshot (`server/index.js:349-393`). Test: **GR-4** | = anon | = anon (token unused here) | = anon (no extra grant on this surface) | Route runs AS service-role; safety = `buildSafePayload` re-stating RLS filters in code (spec §13.5.3:1409-1423). Test: **GR-1/GR-4**; shared safe-select helper + CI payload test OWED (spec:1607) **? → F2** |
| **App SPA** (`app.lock.show`, Vite build) | Public routes only (`/p/:id`, `/confirm/:token`); data reach = the anon-key REST row below. Mech: CSP + HSTS + nosniff + Referrer/Permissions-Policy (`vercel.json:14-21`); `frame-ancestors 'self' lock.show` anti-clickjacking (`vercel.json:15`). Test: headers live-probed 15 Jul (spec:1447) but no CI probe **? → F3** | Own-org data via supabase-js (anon key + user JWT → RLS row below). Session JWT in localStorage — XSS-theft class OPEN, CSP is first line, BFF/httpOnly OWED (spec §13.5.5:1464-1467). Test: **? → F4** | = anon | `/confirm/:token` page; all state via server confirm routes (row below) | Operator UI reads via `is_operator()` RLS (row below) | **Never present** — service key is server-only env (`SUPABASE_SERVICE_ROLE_KEY`, `server/index.js` env; never in client bundle). Test: **? → F5** |
| **Server API routes** (`server/index.js`, deployed `api/index.js`) | Allowed: `/api/health` (:164), passport GET (:433), `POST /api/passport-signal` (:468), `POST /api/availability-request` (server-authored bell), confirm routes. Everything else → 401 `requireAuth` (:126-138). Cross-cutting: 30 req/min/IP sliding window (:43, :90 — in-memory, resets on cold start, spec:1472); body ≤100 KB (:104); field ≤2000 chars (:112-117); CORS allowlist (:66-71). Test: **SD-1, SD-2, SD-5, SD-6, SD-7, SD-8** | Own artist only: `requireArtistOwner` = `artists.created_by === req.userId` (:142-159) on process-evidence (:222), publish (:409), request-confirmation (:721); `/api/notify` closed type-enum + ownership (:555-566); AI caps 15/job, 15/user/day, $50/mo estimate (spec §13.5.4:1427-1443). Test: **SD-3, SD-4** | = anon; `POST /api/availability-request` → 2xx, notification body server-authored, caller text excluded. Test: **SD-6** | `GET/POST /api/confirm/:token` (:747, :772): reach = claim text + artist `stage_name` only (:758-764). Mech: unguessable `randomUUID` token (:722); TTL 14 d → 410 `link_expired` (:739-743, :757, :783; undatable row fails CLOSED :740-742); single-use idempotency (:792-797); response enum (:805); only `yes` earns `producer-confirmed` (:814). Test: **? → F6** (no confirm-token denial cases in SD) | May notify cross-user via `isOperator()` (:549-566). Test: SD-4 proves NON-operator denial; operator-ALLOW path unproven **? → F7** | The server **is** this actor (admin client bypasses RLS). Every gate re-checked in code; residual risk = any new read path that forgets a filter has no RLS backstop (spec §13.5.3:1421-1423). Test: **SD-7** (sanitized errors) + **GR-4**; CI payload test OWED **? → F2** |
| **Supabase REST / RLS** (PostgREST, anon key or user JWT, direct) | **Rows:** published artists only; claims additionally `passport-ok` + `verified/supporting` + **`artist_approved=true`** (`031_passport_approval_gate.sql:23-30` — the load-bearing gate, spec §13.5.2). **Columns:** buyer-safe grant lists only — no `internal_confidence`/`whatsapp_number`/`created_by` (`016_firewall_column_security.sql:14-28`); no `gigs.exact_count`/`settlement_band`, no `act.contact` (`025_firewall_grants.sql:30-44`). **Zero read:** `evidence_artifacts` (spec:1377), `share_link`/`analytics_event`/`passport_view_event` (`025:55-57`), `waitlist_signup` (spec:1392). **Open INSERT:** `professional_reaction`, `analytics_event`, `waitlist_signup`; `passport_signals`/`passport_view_event` only vs published artist (spec:1381-1392). Test: **GR-1** scans DB columns; no live anon-key probe **? → F8** | Org/tenant scope via `owns_artist` (030) / `can_access_artist` (008/027) / `current_org_ids` (spec §13.5.1:1367-1370); catalog per table spec:1372-1393. Test: **? → F9** | = anon (a buyer holds no account; same anon key) | **No direct DB path** — `producer_confirmations` has no anon policy; all writes server/service-role (spec:1382); default-deny RLS. Test: **? → F8** | Read-mostly via `is_operator()` policies (`003_operator_admin.sql:20-41`; audit 011; selective read 012); moderation update on `artists` (`003:35-37`) | Full bypass **by design** — must only be exercised through the server surface above (key never leaves server env) |
| **Site** (`lock.show`, `website-next/` static export) | Public marketing content + `/waitlist` (anon INSERT open, read revoked — migration 026, spec:1392). Mech: nosniff/Referrer/Permissions on all paths + CSP with `frame-ancestors 'none'` on non-`/app` paths (`website-next/vercel.json` `headers`); `/app/*` excluded so the embedded app's own CSP (`vercel.json:15`) governs it. NOTE: spec:1462 still says site headers OWED — the config now EXISTS in-repo but per lesson **L-3** it is unproven until a live probe **? → F10** | = anon (site has no auth) | = anon | = anon | = anon | Not present (static export, no server key) |

## Test legend
- **SD-n** = `scripts/test-security-denial.mjs` case *n* (cases listed :9-16): SD-1 no-auth→401 (:146-155) · SD-2 garbage JWT→401 (:158-161) · SD-3 cross-user ownership→403/404 (:164-173) · SD-4 notify enum + cross-user→403 (:177-181) · SD-5 CORS deny (:184-191) · SD-6 anon availability-request, server-authored bell (:194-208) · SD-7 sanitized 500s + leak-regex sweep (:211-218) · SD-8 rate-limit burst→429 (:221-232). Runs against the REAL express app with a mock Supabase — it proves **server** behavior, not live RLS.
- **GR-n** = `scripts/test-guardrails.mjs` inspector *n*: GR-1 no person-score in code/DB columns/buyer payload · GR-4 `internal_confidence` never in a buyer payload (header :8-14).

## Follow-ups (every `?` above)
1. **F1** — add SD case: `GET /api/passport/:id` on an unpublished artist → 404.
2. **F2** — shared safe-select helper + CI server-payload firewall test (spec:1607, OWED). Closes the service-role no-backstop risk.
3. **F3** — CI/live probe asserting the app's response headers match `vercel.json:14-21` (lesson L-3).
4. **F4** — BFF/httpOnly-cookie session upgrade (spec:1608 area, OWED); until then localStorage-XSS class is open by design.
5. **F5** — CI check that the built client bundle contains no `SUPABASE_SERVICE_ROLE_KEY`/service key material.
6. **F6** — add SD cases for the confirmer-token: expired token → 410, replayed response → `already_recorded`, invalid response → 400.
7. **F7** — add SD positive case: operator JWT may notify cross-user (currently only the denial half is proven).
8. **F8** — live anon-key PostgREST probe: select forbidden columns/tables (016/025 lists) and assert column-grant denial; includes `producer_confirmations` zero-read.
9. **F9** — authed-tenant isolation probe (user A JWT cannot read org B rows via REST); check whether `scripts/test-act-isolation.mjs` already covers part of this before writing new code.
10. **F10** — live header probe of `lock.show` (site CSP/nosniff) before marking spec:1462 BUILT.

*Sources: `server/index.js` · `vercel.json` · `website-next/vercel.json` · migrations 003/016/025/026/030/031 · `docs/LOCK-PRODUCT-SPECIFICATION.md` §13.5 (lines cited inline) · `scripts/test-security-denial.mjs` · `scripts/test-guardrails.mjs`.*
