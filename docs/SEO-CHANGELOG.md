# SEO CHANGELOG — T-96 SEO/AEO/GEO + Entity-Analytics Rebuild
_Deliverable #14 of the 27 Jul owner task. Newest entries on top. Every change to canonical/robots/sitemap/metadata/JSON-LD/analytics-tagging lands here with its SHA._

## Entry 3 · 27 Jul 2026 · STEPS ②③ BUILT — EVIDENCE RETURNED, NOT DEPLOYED (commits `26d78df`+this)
WWW alignment: lib/site.ts single source; metadataBase/robots/sitemap/JSON-LD @ids/llms.txt all on https://www.lock.show; keywords meta deleted (C4); Organization logo → real spotlight-lens mark (C9); SoftwareApplication url → www/app (C8; app.lock.show stays D3-gated); offers text → /pricing truth, ILS (C7). Indexation: homepage invisible FAQPage DELETED + test now hard-enforces visibility (C5 retired); /app de-indexed CORRECTLY — X-Robots-Tag header (vercel.json) + meta in all 30 embed shells + robots Disallow removed so the directive is crawlable (C12 fixed); 404 noindex (C27); legal ×3 noindex + out of sitemap (D6); demo noindex + out of sitemap + Maya Vale + fictional venues (Club Vela/The Attic Stage/Horizon Gathering) + visible "fictional artist" label (D5); llms.txt: /managers+/production purged (D7), pricing line truthful (C10). Sitemap 14→10 URLs, zero redirecting, zero excluded classes. Tests: ROUTE_POLICY route-class gates (S8), Disallow-return inversion (S5), verify 19/19 green, tsc clean, site build 14 routes.
NOT VERIFIED (needs deploy): live X-Robots-Tag header. FLAGS: embed rebuild would strip shell metas (S8 catches it; durable fix = root index.html, src-side) · homepage still names REAL venues in fictional sample claims (Zappa/Shapira — same D5 class, owner-scoped follow-up) · logo is SVG (PNG export if rich-result matters) · D12 verdict: legacy website/ proven unreferenced + superseded + git-recoverable — cleanup commit authorized-pending-word.

## Entry 2 · 27 Jul 2026 · OWNER RULINGS D1–D12 RECORDED (binding)
D1 GA4: DEPENDENCY — deliver console-setup handoff first (Property A "LOCK — Platform Journey" www+app, ONE production stream, `surface` param distinguishes marketing/auth/app/public_profile · Property B "LOCK — Shop", preserve existing Shopify-Google integration, no duplicate page_view/checkout/purchase); owner returns Measurement IDs; NO production ID substitution until then. · **D2 CANONICAL = WWW** (https://www.lock.show; apex 308→www preserved path+query — already live behavior, no DNS change; ALL canonicals/sitemap/robots-ref/OG/JSON-LD @ids move to www; task's apex spec is corrected wherever cited). · D3 app.lock.show migration APPROVED CONDITIONALLY — no /app/* redirect until the 11-item auth/link test list passes + migration & rollback plan returned. · D4 public artist profiles NOINDEX during pilot; no Person/MusicGroup/Event schema until visibility controls+consent+field rules+deindex tests exist. · D5 demo profile: NOINDEX + visibly fictional + REPLACE real venue/festival names; no factual-implying schema. · D6 legal/accessibility: NOINDEX + out of sitemap until owner supplies facts + review completes; never invent facts. · D7 /managers + /production: purge from llms.txt + discovery inventory NOW; no thin pages; check traffic/backlinks before any URL deletion. · D8 Resources: build system (templates/metadata/author/reviewed/sources/draft-state/Article+Breadcrumb support), publish NOTHING thin; first content cluster + search-intent map returned before any article. · D9 positioning CONFIRMED for marketing/SEO (career growth + Artist Universe lead; readiness/opportunity-fit = outcomes; evidence/confirmation = trust layer; no invented features, no guaranteed outcomes; capability-to-claim matrix precedes visible copy). · D10 migrations 034+040 NOT approved — full per-migration card required (purpose/tables/SQL/risk/backup/rollback/staging/T-96-necessity). · D11 AI crawlers: NOT one class — bot-by-bot table (agent/operator/purpose/policy/GEO-effect/source) before changes; preserve current behavior. · D12 legacy website/: conditional cleanup — prove unreferenced (build/CI/imports/docs/live assets/git-recoverable) then isolated "legacy cleanup" commit.
EXECUTION ORDER LOCKED: ① record ② www test-expectations ③ indexation-risk fixes not gated on legal/GA4 (invisible-FAQ removal · noindex private routes · sitemap excludes legal+demo · www alignment · route-class tests) ④ test evidence BEFORE deployment ⑤ no host/auth deploy until evidence passes ⑥ GA4+GSC handoff docs ⑦ repositioning copy only after capability-to-claim matrix.

## Entry 1 · 27 Jul 2026 · REGRESSION HARNESS LANDED (step 1 of the safe order)
`scripts/test-seo-contract.mjs` (S1–S7: unique metadata + canonical-host consistency [host-agnostic — survives the D2 ruling] + sitemap/robots contract + built-HTML ld+json validity + FAQPage visibility w/ C5 allowlisted behind `C5_HOMEPAGE_FAQ_VISIBILITY_ENFORCED=false`) · `scripts/test-analytics-contract.mjs` (A1–A4: CANON⊆CHECK, EVENTS∈CANON∪DEV_ONLY, firewall prop-key scan over 35 call sites) · orphaned `test-site-nav.mjs` wired as `test:sitenav` (passes unmodified) · `.github/workflows/verify.yml` — CI now runs the FULL 19-check verify chain (nothing ran verify in CI before). All 19 green at commit.

## Entry 0 · 27 Jul 2026 · PHASE-0 PRE-CHANGE BASELINE (discovery @ `5fcf7d2`, read-only)
**Surfaces:** marketing = Next.js 16 static export (`website-next/`, project lock-site, lock.show/www) · app = Vite SPA (repo root, project lock-app, app.lock.show) · committed embed mirror at `website-next/public/app` served at lock.show/app · legacy dead `website/` dir (7 files).

### The 30 contradictions found (C1–C30, full cites in the T-96 Phase-0 agent report)
C1 all code signals (metadataBase, robots Host, sitemap, 14 canonicals) point at APEX while live serves WWW (redirect lives in Vercel domain settings, not repo; DEPLOY.md wants www→apex, ops docs use www) · C2 sitemap ships 14 redirecting URLs · C3 `lastModified: new Date()` on every deploy · C4 deprecated keywords meta incl. the WRONG actor (אמרגן) contradicting the site's own FAQ · C5 homepage FAQPage JSON-LD has NO visible Q&A counterpart (policy violation, manual-action risk) · C6 three unconnected FAQPage entities, duplicated questions, no @ids · C7 SoftwareApplication offers text contradicts /pricing ("by arrangement" vs "Free during the pilot") · C8 SoftwareApplication url = marketing homepage, not the app · C9 Organization logo = a 1200×630 OG card (not a logo) · C10 llms.txt advertises nonexistent /managers + /production and denies live /pricing · C11 llms.txt asserts apex · C12 /app/* Disallowed in robots but NO meta/header noindex anywhere (0 noindex in shippable code) · C13 app.lock.show: no robots.txt, every path 200s (soft-404 farm; /passport /confirm /invite crawlable) · C14 /passport/demo indexable at priority 0.8 with fabricated artist + REAL venue names, no sample disclosure in schema · C15 ONE GA4 property (G-ZX907M2NY8, stream "LOCK App") mixes site + embed + standalone app · C16 app GA ID hardcoded, no env override · C17 canon `login` fires on passive session restore (funnel inflation; GA4 name collision) · C18 ZERO site-side GA4 events (UTM-only handoff) · C19 no event_id/dedup/join key anywhere (GA4↔Supabase unreconcilable) · C20 5 CANON events silently localStorage-only (034+040 unapplied) · C21 marketing site ships zero security headers · C22 positioning: proof×1905 + Passport×3139, career-growth language nowhere; growth-intelligence brand asset unused · C23 www missing from BuildStamp LIVE_HOSTS (live app stamps PREVIEW) · C24 hardcoded apex links inside the app (extra 308 hop) · C25 server email hardcodes app.lock.show while ops docs use www/app (drift class) · C26 no hreflang, lang="en" hardcoded despite runtime HE (zero Hebrew SEO in an Israel-declared market) · C27 404 page has no metadata/noindex · C28 12 bespoke OG images unused (9 pages share one generic card) · C29 legacy website/ dir · C30 zero SEO tests; test-site-nav.mjs orphaned; NO CI runs verify at all.

### Owner decisions D1–D12 (delivered to owner 27 Jul — status tracked here)
D1 GA4 property split (create A platform + B shop, new IDs; accept reporting discontinuity) · D2 apex-vs-www canonical (task says APEX; discovery recommends WWW = zero churn; one-way door — awaiting explicit confirm) · D3 app.lock.show migration timing (10 auth-link gates incl. already-sent magic/share links; after D2) · D4 public-passport indexation policy (privacy/Amendment-13) · D5 /passport/demo indexability + fabrication disclosure (real venue names in fake evidence) · D6 legal pages noindex-until-final · D7 /managers + /production: build or purge from llms.txt · D8 Resources/editorial GO + content owner · D9 positioning (RULED by the task itself: career-growth leads on marketing layer; product canon/firewall untouched) · D10 apply migrations 034+040 (existing pending item) · D11 AI-crawler policy (GPTBot/ClaudeBot/Perplexity/CCBot: allow?) · D12 delete legacy website/ dir.

### Safe execution order (locked)
1 regression harness FIRST (test-seo-contract + test-analytics-contract + wire test-site-nav + CI verify workflow — RUNNING) → 2 Phase-1 canonical/host truth in code (after D2/D9) → 3 Phase-2 indexation (X-Robots-Tag BEFORE relaxing Disallow; noindex 404/demo/legal per D4-D6) → 4 Phase-3 schema graph (delete unsupported homepage FAQPage EARLY — manual-action risk) → 5 Phase-4 analytics (gated D1+D10; event_id + join key; login-on-restore fix LAST on a clean property) → 6 Phase-6 app-domain migration (gated D3; env-var flip; permanent 308; never remove old Supabase allowlist origin).
One-way doors: the D2 domain flip · Supabase allowlist removals. Cheapest levers: NEXT_PUBLIC_APP_URL env var (site-side app-origin flip needs zero code).
