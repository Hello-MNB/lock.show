# LOCK — Launch Plan to Gate 1 (the free-pilot spine)
_Owner-ordered (21 Jul 2026): the current instruction set works ONLY from this doc. Free pilot = FULL app, payment off — free is the pricing, not the scope. Landed from the module breakdown so every "work from the plan" reference resolves (INDEX top rule)._

## The ruling
Free real-user signup — no pay screen, no card. First cohort = the owner's real account + invited real artists (real, never demo). All texts + signals run LIVE. Payment (M-8) stays OFF — post-pilot. The app is otherwise COMPLETE (STEP-0 audit T-85 proved entity×screen + nav completeness).

## Modules
| # | Module | Status (21 Jul) |
|---|---|---|
| 1 | **Radar redesign A+B** (owner pick: coach card + plain-name shelf + ambient radial, §8.2 ruling block) | BUILT — preview `91802cb`, awaiting witness |
| 2 | **Artist entity complete, high bar** (11 screens, T-85 Table 1) | BUILT — same preview, awaiting witness |
| 3 | **Per-user measurement** — per-persona signals; buyer decision funnel first (§14 extension: passport_opened{face} · proof_unit_expanded · method_label_peeked · persona_toggled · availability_request_started vs _submitted), then Rep/Production. Firewall: ids/roles/context only; buyer behavior feeds the operator cockpit, never returns to the artist as a count | Audit delivered; wiring lane DISPATCHED 21 Jul |
| 4 | **Marketing-site signup fix + rebuild** — real artists must get IN (site→signup→onboarding E2E); site design = owner-approved direction C (v4) | Signup lane DISPATCHED 21 Jul; design build follows the v4 verdict |
| 5 | Pilot polish: confirmer "partly" correction box (§8.9) · demo persona → Maya Vale in code · site sample rename | DISPATCHED 21 Jul |

## Order
1 ✅ ruling confirmed → 2 ✅ STEP-0 audit → 3 (now) close pilot gaps, max parallel → 4 owner witnesses Modules 1+2 → merge → 5 owner real-account test (build → publish → open as buyer → all personas' signals visible in cockpit) → 6 ship free to first real users → measure the pilot window.

## Laws that bind every module
Spec read FRESH per task (§ + line cited) · status in TASK-REGISTER only · firewall absolute · witness gates the merge · payment flag untouched · migrations authored, never run (owner applies).
