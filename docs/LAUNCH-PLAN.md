# LOCK — LAUNCH PLAN A→Z
**v1.0 · 9 Jul 2026 · Owner: Maria (R00) · Principle: measure willingness-to-pay BEFORE building billing (canon: Gate = 1 booking manager reacts + one pays; monetisation measured, not required). Green Invoice = deferred until first payment intent (owner ruling 9 Jul).**

## A · FOUNDATION — DONE ✅
Product live (app.lock.show): onboarding→radar universe→evidence→AI labeling→claims→passport→requests→payment-reference→notifications · Google login · consent/GA4 compliant · legal drafts published · brand LOCK end-to-end.

## B · SITE LIVE (in flight, days=0)
- ME: marketing site serving (fix in Vercel queue; plan-B clickable by Cowork) → Cowork submits sitemap in Search Console.
- Exit: lock.show 200 + LOCK title + pricing page reachable.

## C · INSTRUMENTATION SPRINT (mine, ~1-2 days) — the WTP engine
1. Maria pastes **027** (2 min) → I apply **028** right after (same artifact flow) — adds the event taxonomy.
2. I build the **M1 event writers** (app emits: signup, onboarding, radar_opened, evidence_added, claim_confirmed, passport_published, pricing/upgrade interactions, payment_reference_created…), mirrored to GA4 + first-party analytics_event.
3. **WTP SIGNALS — the ladder** (what we read, weakest→strongest):
   - S1 soft: /pricing views · plan-card expands · time-on-pricing
   - S2 medium: "Upgrade" CTA clicks in-app · Momentum plan opens · repeat pricing visits
   - S3 strong: **payment_reference_created** (artist generated a Bit GP-code = typed intent)
   - S4 GATE: Bit payment actually received (manual confirm) + booking-manager reaction on a real passport
4. I deliver a weekly-readable **WTP mini-dashboard** (GA4 exploration + one SQL view) — bands and counts of events, never artist scores.
- Exit: every S1–S4 signal visibly recorded from a test run.

## D · SEED COHORT (Maria-led, week 1)
- 5–10 real artists from your network onboard on app.lock.show (each ≤10 min — the radar does the work).
- ME: watch funnels, fix friction same-day (analytics show where they drop).
- YOU: personal invite messages (I draft HE templates on request).
- Exit: ≥5 published passports with ≥2 proof units each.

## E · BUYER MOMENT (week 1-2) — the Gate test
- Each artist shares their passport with 1–3 real אמרגנים/מפיקים (WhatsApp — flow exists).
- Signals watched: passport_view → professional_reaction → availability_request (this IS the Gate's first half).
- Exit: first real manager reaction recorded.

## F · MONETIZATION TEST (week 2) — price shows, money measured
- Price goes visible as **Founding Pilot ₪179** (or your number — still awaiting the word) on pricing + upgrade screens.
- S3 fires (GP-code generated) → THEN activate Green Invoice (15-min signup at that moment, not before — your hold honored).
- First Bit payment + receipt = **GATE COMPLETE** = the business is validated.
- Exit: 1 payment. (Then: plan_flags enforcement I've already prepared in 028.)

## G · HEBREW LAUNCH (week 2-3)
- The measured work order: 152 app strings + 8 site pages + native-editor pass + home-page i18n wiring.
- Exit: full HE product → your launch announcement (socials H1-H4 claimed by then).

## H · GROWTH LOOPS (post-launch, already spec'd)
Discovery engine (Tavily ✅, counsel-gated) · attributed share loop · Spotify/YouTube context · value-vs-genre (after taxonomy rulings) · Resend branded emails · production-workspace depth.

## DECISION GATES (when to advance)
B→C: site serves · C→D: signals recording · D→E: 5 passports · E→F: first reaction · F→G: first payment OR 2 weeks of S2+ signals (price-test iterate) · G→launch: native HE pass done.

## YOUR TOTAL HANDS-ON (whole plan)
027 paste (2 min) · price word · seed invites (your network) · Green Invoice only-when-S3 · counsel email · HE native review session.
