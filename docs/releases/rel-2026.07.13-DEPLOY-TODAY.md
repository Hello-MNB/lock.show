# rel-2026.07.13 — TODAY'S DEPLOY VERSION (owner order: launch to air today + QA)
**Scope = branch head at declaration (everything BUILT and gate-green; npm verify exit 0).**
## IN (built, on branch, gates green)
Site: terminology canon everywhere · /managers + /production outreach pages · waitlist role+
source_page+?src attribution (033) · pricing unpublished (S8) · OG/social images wired · brand
assets incl. 18 source-badge SVGs. App: post-M8 state-derived next-action (reply→refresh→share)
· genre guidance line · multi-Act + create-act · roster-grants + production-requests (032) ·
9+1 pilot events · terminology waves EN/HE · public passport no-login + availability requests.
Docs of record: flows per entity, admin spec, registries F1+deltas with validators.
## OUT (explicitly NOT in today's version — next version)
Radar milestone-path + genre-emphasis UI (in build) · A2 add-workspace · A5 roster widget ·
S2–S4 site fixes · admin Business cockpit · literal-extraction wave (🚩loc flag stays open —
today's literals ship AS ALREADY LIVE TODAY, no regression) · migration 034 apply (owner).
## Train (per rel-PLAN §4b)
1. THIS declaration = PREVIEW-READY for the above scope. 2. Maria's banked "preview go" fires →
Cowork preview deploy. 3. Q1–Q8 on preview vs FLOWS-PER-ENTITY (incl. new-user signup + existing-
user login forward/backward paths). 4. Fixes if found → re-QA. 5. Maria Q8 owner pass → atomic
merge → live re-verify → tag + rollback anchor.
