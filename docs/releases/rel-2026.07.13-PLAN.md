# rel-2026.07.13 — Gap-Closing Release Plan · PER-ENTITY · with binding post-release QA/QC

_Planned 12 Jul, restructured same day on owner order: release plans must be per-entity
(site: navigation · content · images · messages · responsive fit · UX/UI+brand check;
app: navigation · content · mobile-first fit · messages · UX/UI), plus required
technologies/integrations, plus a QA/QC protocol after publish — **a version that has not
passed the QA/QC protocol is NOT a valid version.**
Basis: FLOW-GAP-AUDIT-2026-07-10 · UX-JOURNEY-AUDIT · ENTITY-GLOSSARY + GLOSSARY v1.1 ·
DS v1.6.0 (platform/entity matrix + per-entity DOD) · Codex audit ranks (site 88 · app
desktop 84 · app mobile 80 · imagery 76 · OG 68)._

## 0 · Terminology wave — BUILT on branch (deploys with this release, after owner Hebrew nod)
Buyer ≠ אמרגן purge (app 8 HE + 7 EN strings, signup labels, site glosses/faq/llms.txt,
GLOSSARY v1.1, CLAUDE.md) · forbidden-term דרכונים removed (3 UI strings) · SIGNUP_ROLES
producer removed (Source Confirmer ≠ signup, DS v1.6.0 issue #4) · evaluation-language softened
on artist-facing + job-select strings (context/fit/confidence per DS voice). All builds green.

## 1 · SITE — per entity (lock.show)

| Entity page | Navigation | Content/Messages | Images | Responsive | UX/UI + brand check |
|---|---|---|---|---|---|
| Home (all) | nav: Artists · Booking Managers · Producers (About later) — **Pricing REMOVED from nav per S8** | persona cards per ratified vocabulary; sentiment per audience | lockshow-hero-live + persona set | full-bleed hero ≤92svh; mobile m-flat white-screen | DS v1.6.0 home DOD; single lime CTA rule |
| Artists | breadcrumb + cross-links to pricing/demo | "supported, not inspected" voice; proof-building story | persona-artist + atmosphere PNG (awaits files) | hero + card stack mobile | DOD: artists page |
| Booking managers (/bookers) | keep slug; copy = professional buyer | professional register; + ONE line acknowledging private/corporate clients (no jargon); never "amargan"; ⭐ booker may be a PRIVATE individual (wedding couple) — warm direct copy at that register, no venue jargon | persona-manager; buyer-context image (Codex brief open) | 2-min-read layout mobile | DOD: professional buyer |
| Private client | **NOT a page this release** — represented via /bookers line + FAQ entry | simple language: style, fit, trust, availability | Codex imagery brief (missing material) | — | post-validation page; backlog |
| Producers (/producers) | keep slug | + clarifying line: this is the Source Confirmer flow (magic link, no account); production COMPANY is a different thing | persona-producer | 20-second story mobile | DOD: production/source |
| Pricing (all 4 entities) | ⛔ **SUPERSEDED by SCOPE S8 (owner word 12 Jul night): page UNPUBLISHED** — removed from nav/footer/sitemap; /pricing → /artists redirect; file kept for one-commit restore post-Gate | — | — | — |
| Radar / Passport demo | deep links | method-labeled evidence story; NO-SCORE as internal value not negation wall | evidence-review image | demo passport mobile pass | firewall visual check |
| OG/social (all) | — | per-platform export rules now in DS | **missing files — Codex/owner export (rank 68)** | — | brand check on each export |

## 2 · APP — per entity (app.lock.show) · MOBILE-FIRST

| Entity | Navigation | Content/Messages | Mobile-first fit | UX/UI correctness |
|---|---|---|---|---|
| Artist | home = one smart next-action widget; + **New Act** entry | supported-not-inspected voice (evaluation language removed ✓); producer-confirm request wired to REAL magic link | 360px: one decision per screen, sticky primary action, no dense tables | DS entity-aware rules: declare workspace, role, act, privacy state, next action |
| Manager office (אמרגן) | roster home gets **next-best-action** | roster = booking agents team vocabulary ✓; membership ≠ ArtistAccess visible | roster list → mobile cards | seats/invites flows re-checked |
| Production workspace | requests inbox with REAL data (migration 032) | never called bare "producer" in UI | inbox mobile cards + sticky respond | separate from Source Confirmer everywhere |
| Booker (recipient) | discover + Passports tab ✓ (R1) | מזמין הופעות vocabulary ✓; review needs no workspace | Passport read: 2-minute mobile scan, bands+binaries legible | two-persona views (?view=rep) verified |
| Source Confirmer | NO nav (magic link only) | one-claim confirm screen, 20 seconds | one-tap confirm mobile | never gets dashboard/workspace shell (SIGNUP_ROLES fixed ✓) |
| Operator | admin console | **activated_by** audit trail on activation | desktop-first OK (internal) | payment→entitlement→event chain |
| All | "Add workspace" made REAL (flow-gap P; only artist/management/production get workspaces) | remaining funnel events (11) | A13 token binding = mobile-first re-ground batch | DS v1.6.0 audit gate before binding |

## 3 · Technologies / integrations required
| Item | For | Status |
|---|---|---|
| Migration 032 (roster-grants RPC + production-requests RPC) | app §2 rows 2–3 | Claude authors → Cowork applies → regression |
| Resend SMTP | email confirm delivery (QA item Q3) | Cowork, after owner signup |
| Google OAuth click-test / Facebook app | signup flows | owner (checklists ready) |
| A13 token values + DS v1.6.0 | token binding | delivered; my independent audit = gate |
| Physical assets into git (logos, icons, atmosphere PNGs, OG exports) | site §1 images | owner/Codex — only design-side blocker |
| Embed rebuild (build:embed) | lock.show/app parity | mandatory same-release (standing rule) |

## 4 · QA/QC PROTOCOL — TWO STAGES; **version invalid until ALL pass**
**Stage 1 — Candidate qualification (Q1–Q7 on the immutable preview SHA).** All seven lanes run
against the frozen candidate SHA on the isolated preview — never against production.
**Stage 2 — Production acceptance (live smoke + Maria's Q8 owner pass).** After the atomic merge:
live-smoke re-check of Q2/Q5/Q6 fingerprints on production, then Q8 — assigned to Maria ONLY
(no delegate, no proxy sign-off).
| # | Check | Who | Evidence required |
|---|---|---|---|
| Q1 | Pre-deploy gate: `npm run verify` (nav 34 · i18n purity · build · demo build) + `next build` | Claude | CI output in release doc |
| Q2 | Live fingerprints: served JS/pages contain this release's markers; embed hash matches app | Claude | fingerprint list + hashes in DEPLOY-LOG |
| Q3 | Real-browser flows (C1–C2): signup (email + Google), artist journey, booker link open, workspace switch, producer magic link | Cowork | screenshots + pass/fail table |
| Q4 | Mobile pass: 360px screenshots of Radar, roster, Passport, recipient flow (Codex's app-mobile rank 80 items) | Claude (Playwright demo) + Cowork (real devices) | PNGs into docs/design-system/current-screens/ |
| Q5 | Firewall scan on live surfaces: no score/%/rank/gauge; bands+binaries+method labels only | Claude | grep + visual check noted |
| Q6 | Terminology scan on live surfaces: 0 forbidden terms, 0 buyer=אמרגן | Claude | scan output |
| Q7 | Version governance: VERSIONS.md + DEPLOY-LOG + roadmap artifact updated; ledger row added as "technically verified (evidence named)" | Claude | C6 cross-audit (Cowork) |
| Q8 | Owner approval pass → ledger column flips to approved (Stage 2 — production acceptance) | **Maria ONLY** (sole assignee; never delegated) | her word, recorded |
Rollback rule: any Q1–Q6 failure on live → immediate redeploy of previous SHA (recorded in
DEPLOY-LOG), fix on branch, re-run protocol from Q1.

## 4b · RELEASE-TRAIN STRATEGY (owner intent: one clean cross-cutting version, no breakage)
1. **Lock canon first** — the 2 open Hebrew rulings (ENTITY-GLOSSARY §2d) + wave-A wording nod. Nothing builds on shifting vocabulary.
2. **One branch = one train** — everything (terminology + UX/UI + site + app) rides `claude/b4-gigproof-discovery-e7749o`; no partial pushes to main.
3. **Prove on preview before production** — one-shot Vercel preview deploy of the branch (previews are OFF globally for quota; Cowork creates a one-time preview hook or temporarily re-enables for this branch). Full Q1–Q8 runs on the preview URL. Production is never the test surface.
4. **DB safety sequencing** — migration 032 is ADDITIVE (2 RPCs, no destructive change) → safe now. Structural renames (033+: workspace_type 'producer'→'production', enums) wait for **Supabase Pro backups ON (owner, $25/mo)** — backups are the prerequisite for any structural migration, not for 032.
5. **Atomic go-live** — merge → main = one production deploy per project (smart build-skip keeps it to 2 deploys total). No half-states; embed rebuilt in the same push.
6. **Post-deploy QA/QC** — protocol §4 (Q1–Q8) re-run against LIVE; Cowork owns Q3–Q5 lane.
7. **Tag + rollback** — previous SHAs recorded pre-merge in DEPLOY-LOG; any live Q-failure → instant redeploy of previous SHA. Tags pushed by Cowork/owner (my token can't: 403).

## 5 · Explicitly NOT in this release
Enum/DB renames (workspace_type 'producer'→'production', role enum names) = migration 033+ ·
Hebrew site · About page (owner facts) · private-client site page (post-validation) · comparison
mode (firewall watch) · Radar v5 emotional layer.
