# Prototype Changelog — newest first

## 2026-07-10 — prototypes brought under version management
- Established `docs/prototypes/` governance (`00_CURRENT` + `04_VERSIONS` + `REGISTRY.md` + `VERSION.json`).
  Reason: a gap in current, versioned prototypes lets design partners (Codex) guess — owner directive to
  put the prototypes in order, with version management.
- **Radar v4** — momentum/"career-kick" framing; method ladder (Missing→Self-declared→Source-linked→
  Evidence-supported→Producer-confirmed); real platform logos + CSS media scenes; stage lighting. Current.
- **Passport v3** — mobile-first, two buyer personas (Booking · Representation), sticky tabs, facts-only
  cards, one CTA per view; all explanatory text removed. Current. Shipped into the app in rel-2026.07.10.
- **Version Roadmap v1** — users × releases matrix, in the DS look. Current.
- **Flow Map v1** / **Release Guide v1** — architecture + release-process explainers. Current.
- Archived **GIGPROOF Desktop v0-final** (pre-development, pre-LOCK) to `04_VERSIONS` — superseded by Radar
  v4 + Passport v3.

_Earlier prototype iterations (Radar v1–v3, Passport v1–v2) were live-edited in place before this registry
existed; from here on every viewer-visible change bumps the version and lands a line here._
